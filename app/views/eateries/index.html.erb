<section class="eateries">
  <p class="resource-actions">
    <%= link_to "Add", new_eatery_path %>
  </p>

  <ul>
    <% @eateries.each do |eatery| %>
      <li>
        <%= link_to eatery_path(eatery) do %>
          <%= eatery.name %>
          <span class="recommendation-count"><%= eatery.recommendations.count %> ♥︎</span>
        <% end %>
      </li>
    <% end %>
  </ul>
</section>
